-- DB SETUP
CREATE DATABASE fakeshop IF NOT EXISTS;


-- FIXME: If no firstname/lastname or user_address exists --> no possible to do payment.
-- UNIQUE(table(FK)) -> setup unique constraint, it mean that just this single row can have relation to for from FK column.
-- \i /Users/karolguzik/Downloads/file.sql  -> command to create all queries from sql file
-- \x it change displaying view with command results for column schema


